# Refuse to install under a Node version that does not match the `engines` pin
# (24.x) instead of installing happily and failing later at runtime — supabase-js
# needs native WebSocket, which does not exist before Node 22.
engine-strict=true
